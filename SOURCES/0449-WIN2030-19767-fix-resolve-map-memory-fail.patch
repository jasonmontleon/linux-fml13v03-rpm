From 95f4592fe6d87a31f5e79cab029f6bff23b0928e Mon Sep 17 00:00:00 2001
From: fanxiaping <fanxiaping@eswincomputing.com>
Date: Sat, 11 Oct 2025 15:28:58 +0800
Subject: [PATCH 449/540] WIN2030-19767:fix:resolve map memory fail

Changelogs:
        reslove map memory fail

Change-Id: Icbb4dfa3d3dba92e957fa7ff4ec2255cad88593d
Signed-off-by: fanxiaping <fanxiaping@eswincomputing.com>
---
 arch/riscv/configs/eic7700_dbg_defconfig     |  1 -
 arch/riscv/configs/eic7702_dbg_defconfig     |  1 -
 arch/riscv/configs/eswin_aicard_defconfig    |  1 -
 arch/riscv/configs/m2_evb_defconfig          |  1 -
 arch/riscv/configs/u2_evb_defconfig          |  1 -
 arch/riscv/configs/vpu7702_evb_defconfig     |  1 -
 arch/riscv/configs/vpu7702_pcie_a2_defconfig |  1 -
 arch/riscv/configs/vpu7702_pcie_defconfig    |  1 -
 arch/riscv/configs/win2030_defconfig         |  1 -
 drivers/gpu/drm/eswin/Makefile               |  4 +-
 drivers/gpu/drm/eswin/es_crtc.c              |  8 ---
 drivers/gpu/drm/eswin/es_dc.h                |  2 -
 drivers/gpu/drm/eswin/es_dc_hw.c             |  2 -
 drivers/gpu/drm/eswin/es_dc_hw.h             | 11 +---
 drivers/gpu/drm/eswin/es_drv.c               |  2 -
 drivers/gpu/drm/eswin/es_drv.h               |  5 --
 drivers/gpu/drm/eswin/es_gem.c               | 64 ++++----------------
 drivers/gpu/drm/eswin/es_gem.h               |  4 --
 18 files changed, 17 insertions(+), 94 deletions(-)

diff --git a/arch/riscv/configs/eic7700_dbg_defconfig b/arch/riscv/configs/eic7700_dbg_defconfig
index d59931be3433..7081d3e8fe1d 100644
--- a/arch/riscv/configs/eic7700_dbg_defconfig
+++ b/arch/riscv/configs/eic7700_dbg_defconfig
@@ -657,7 +657,6 @@ CONFIG_DRM_TOSHIBA_TC358762=y
 CONFIG_DRM_DW_MIPI_DSI=y
 CONFIG_ESWIN_MIPI_DSI=y
 CONFIG_ESWIN_RASPBERRYPI_7INCH_PANEL=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
diff --git a/arch/riscv/configs/eic7702_dbg_defconfig b/arch/riscv/configs/eic7702_dbg_defconfig
index e0fe69d5a784..47715ce2803e 100644
--- a/arch/riscv/configs/eic7702_dbg_defconfig
+++ b/arch/riscv/configs/eic7702_dbg_defconfig
@@ -661,7 +661,6 @@ CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
 CONFIG_DW_HDMI_CEC=y
diff --git a/arch/riscv/configs/eswin_aicard_defconfig b/arch/riscv/configs/eswin_aicard_defconfig
index 920d04bd4f03..916aa1b6c496 100644
--- a/arch/riscv/configs/eswin_aicard_defconfig
+++ b/arch/riscv/configs/eswin_aicard_defconfig
@@ -633,7 +633,6 @@ CONFIG_DRM_SIMPLE_BRIDGE=y
 CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
diff --git a/arch/riscv/configs/m2_evb_defconfig b/arch/riscv/configs/m2_evb_defconfig
index 40d622575f0a..dae9c481aa19 100644
--- a/arch/riscv/configs/m2_evb_defconfig
+++ b/arch/riscv/configs/m2_evb_defconfig
@@ -646,7 +646,6 @@ CONFIG_DRM_SIMPLE_BRIDGE=y
 CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
diff --git a/arch/riscv/configs/u2_evb_defconfig b/arch/riscv/configs/u2_evb_defconfig
index 43d6b8c66a4d..abccc570760a 100644
--- a/arch/riscv/configs/u2_evb_defconfig
+++ b/arch/riscv/configs/u2_evb_defconfig
@@ -659,7 +659,6 @@ CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
 CONFIG_DW_HDMI_CEC=y
diff --git a/arch/riscv/configs/vpu7702_evb_defconfig b/arch/riscv/configs/vpu7702_evb_defconfig
index 5930d3e13b97..ad621ac0c699 100644
--- a/arch/riscv/configs/vpu7702_evb_defconfig
+++ b/arch/riscv/configs/vpu7702_evb_defconfig
@@ -646,7 +646,6 @@ CONFIG_DRM_SIMPLE_BRIDGE=y
 CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
diff --git a/arch/riscv/configs/vpu7702_pcie_a2_defconfig b/arch/riscv/configs/vpu7702_pcie_a2_defconfig
index bf187b0c06b6..fba11e1a7063 100644
--- a/arch/riscv/configs/vpu7702_pcie_a2_defconfig
+++ b/arch/riscv/configs/vpu7702_pcie_a2_defconfig
@@ -645,7 +645,6 @@ CONFIG_DRM_SIMPLE_BRIDGE=y
 CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
diff --git a/arch/riscv/configs/vpu7702_pcie_defconfig b/arch/riscv/configs/vpu7702_pcie_defconfig
index a2dac26c3b11..cb25229901d5 100644
--- a/arch/riscv/configs/vpu7702_pcie_defconfig
+++ b/arch/riscv/configs/vpu7702_pcie_defconfig
@@ -645,7 +645,6 @@ CONFIG_DRM_SIMPLE_BRIDGE=y
 CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
diff --git a/arch/riscv/configs/win2030_defconfig b/arch/riscv/configs/win2030_defconfig
index 3086290618c2..78601701cd89 100644
--- a/arch/riscv/configs/win2030_defconfig
+++ b/arch/riscv/configs/win2030_defconfig
@@ -634,7 +634,6 @@ CONFIG_DRM_SIMPLE_BRIDGE=y
 CONFIG_DRM_TOSHIBA_TC358768=m
 CONFIG_DRM_SIMPLEDRM=m
 CONFIG_DRM_ESWIN=y
-CONFIG_ESWIN_MMU=y
 CONFIG_ESWIN_DW_HDMI=y
 CONFIG_ESWIN_VIRTUAL_DISPLAY=y
 CONFIG_DW_HDMI_I2S_AUDIO=y
diff --git a/drivers/gpu/drm/eswin/Makefile b/drivers/gpu/drm/eswin/Makefile
index 1f7a44346d8d..b350d3e72718 100644
--- a/drivers/gpu/drm/eswin/Makefile
+++ b/drivers/gpu/drm/eswin/Makefile
@@ -9,10 +9,10 @@ es_drm-objs := es_dc_hw.o \
 		es_fb.o \
 		es_gem.o \
 		es_plane.o \
-		es_simple_enc.o
+		es_simple_enc.o \
+		es_dc_mmu.o
 
 es_drm-$(CONFIG_ESWIN_VIRTUAL_DISPLAY) += es_virtual.o
-es_drm-$(CONFIG_ESWIN_MMU) += es_dc_mmu.o
 es_drm-$(CONFIG_ESWIN_DW_HDMI) += es_dw_hdmi.o dw-hdmi.o
 
 es_drm-$(CONFIG_DW_HDMI_I2S_AUDIO) += dw_hdmi_i2s_audio.o
diff --git a/drivers/gpu/drm/eswin/es_crtc.c b/drivers/gpu/drm/eswin/es_crtc.c
index 54b74b638069..c997fba42c11 100644
--- a/drivers/gpu/drm/eswin/es_crtc.c
+++ b/drivers/gpu/drm/eswin/es_crtc.c
@@ -59,9 +59,7 @@ static void es_crtc_reset(struct drm_crtc *crtc)
 	state->sync_mode = ES_SINGLE_DC;
 	state->output_fmt = MEDIA_BUS_FMT_RBG888_1X24;
 	state->encoder_type = DRM_MODE_ENCODER_NONE;
-#ifdef CONFIG_ESWIN_MMU
 	state->mmu_prefetch = ES_MMU_PREFETCH_DISABLE;
-#endif
 }
 
 static struct drm_crtc_state *
@@ -87,9 +85,7 @@ es_crtc_atomic_duplicate_state(struct drm_crtc *crtc)
 	state->dither_enable = ori_state->dither_enable;
 	state->underflow = ori_state->underflow;
 	state->bg_color = ori_state->bg_color;
-#ifdef CONFIG_ESWIN_MMU
 	state->mmu_prefetch = ori_state->mmu_prefetch;
-#endif
 
 	return &state->base;
 }
@@ -350,12 +346,10 @@ static const struct drm_prop_enum_list es_sync_mode_enum_list[] = {
 	{ ES_MULTI_DC_SECONDARY, "secondary dc for multi dc mode" },
 };
 
-#ifdef CONFIG_ESWIN_MMU
 static const struct drm_prop_enum_list es_mmu_prefetch_enum_list[] = {
 	{ ES_MMU_PREFETCH_DISABLE, "disable mmu prefetch" },
 	{ ES_MMU_PREFETCH_ENABLE, "enable mmu prefetch" },
 };
-#endif
 
 struct es_crtc *es_crtc_create(struct drm_device *drm_dev,
 			       struct es_dc_info *info)
@@ -417,7 +411,6 @@ struct es_crtc *es_crtc_create(struct drm_device *drm_dev,
 
 	drm_object_attach_property(&crtc->base.base, crtc->dither, 0);
 
-#ifdef CONFIG_ESWIN_MMU
 	if (info->mmu_prefetch) {
 		crtc->mmu_prefetch = drm_property_create_enum(
 			drm_dev, 0, "MMU_PREFETCH", es_mmu_prefetch_enum_list,
@@ -428,7 +421,6 @@ struct es_crtc *es_crtc_create(struct drm_device *drm_dev,
 		drm_object_attach_property(&crtc->base.base, crtc->mmu_prefetch,
 					   ES_MMU_PREFETCH_DISABLE);
 	}
-#endif
 
 	crtc->max_bpc = info->max_bpc;
 	crtc->color_formats = info->color_formats;
diff --git a/drivers/gpu/drm/eswin/es_dc.h b/drivers/gpu/drm/eswin/es_dc.h
index 8c7c014c7d06..dd99c5fdff6f 100644
--- a/drivers/gpu/drm/eswin/es_dc.h
+++ b/drivers/gpu/drm/eswin/es_dc.h
@@ -28,9 +28,7 @@
 #include "es_plane.h"
 #include "es_crtc.h"
 #include "es_dc_hw.h"
-#ifdef CONFIG_ESWIN_MMU
 #include "es_dc_mmu.h"
-#endif
 
 struct es_dc_funcs {
 	void (*dump_enable)(struct device *dev, dma_addr_t addr,
diff --git a/drivers/gpu/drm/eswin/es_dc_hw.c b/drivers/gpu/drm/eswin/es_dc_hw.c
index 24f95570d56f..46b36128e91b 100644
--- a/drivers/gpu/drm/eswin/es_dc_hw.c
+++ b/drivers/gpu/drm/eswin/es_dc_hw.c
@@ -1511,7 +1511,6 @@ void dc_hw_commit(struct dc_hw *hw)
 	}
 }
 
-#ifdef CONFIG_ESWIN_MMU
 static u32 mmu_read(struct dc_hw *hw, u32 reg)
 {
 	return readl(hw->mmu_base + reg - MMU_REG_BASE);
@@ -1595,7 +1594,6 @@ void dc_mmu_flush(struct dc_hw *hw)
 	mmu_write(hw, MMU_REG_CONFIG, config);
 	mmu_write(hw, MMU_REG_CONFIG, read);
 }
-#endif
 
 void dc_hw_update_roi(struct dc_hw *hw, enum dc_hw_plane_id id,
 		      struct dc_hw_roi *roi)
diff --git a/drivers/gpu/drm/eswin/es_dc_hw.h b/drivers/gpu/drm/eswin/es_dc_hw.h
index 8999e0c382b4..dfe8fafc50e9 100644
--- a/drivers/gpu/drm/eswin/es_dc_hw.h
+++ b/drivers/gpu/drm/eswin/es_dc_hw.h
@@ -22,9 +22,7 @@
 #ifndef __ES_DC_HW_H__
 #define __ES_DC_HW_H__
 
-#ifdef CONFIG_ESWIN_MMU
 #include "es_dc_mmu.h"
-#endif
 
 #define DC_HW_REVISION 0x24
 #define DC_HW_CHIP_CID 0x30
@@ -175,7 +173,6 @@
 #define YUV_TO_RGB_TABLE_SIZE 16
 #define RGB_TO_YUV_TABLE_SIZE 12
 
-#ifdef CONFIG_ESWIN_MMU
 #define DC_MMU_PREFETCH 0x1E98
 
 #define MMU_REG_BASE 0x0180
@@ -190,7 +187,6 @@
 
 #define DEC_REG_CONTROL 0x0800
 #define DEC_REG_CONTROL_VALUE 0x02010188
-#endif
 
 enum dc_hw_chip_rev {
 	DC_REV_5551,
@@ -451,9 +447,8 @@ struct dc_hw {
 	enum dc_hw_out out;
 	void *hi_base;
 	void *reg_base;
-#ifdef CONFIG_ESWIN_MMU
 	void *mmu_base;
-#endif
+
 	struct dc_hw_read *read_block;
 	struct dc_hw_display display;
 	struct dc_hw_gamma gamma;
@@ -484,11 +479,11 @@ bool dc_hw_check_underflow(struct dc_hw *hw);
 void dc_hw_enable_shadow_register(struct dc_hw *hw, bool enable);
 void dc_hw_set_out(struct dc_hw *hw, enum dc_hw_out out);
 void dc_hw_commit(struct dc_hw *hw);
-#ifdef CONFIG_ESWIN_MMU
+
 int dc_hw_mmu_init(struct dc_hw *hw, dc_mmu_pt mmu);
 void dc_hw_enable_mmu_prefetch(struct dc_hw *hw, bool enable);
 void dc_mmu_flush(struct dc_hw *hw);
-#endif
+
 void dc_hw_update_roi(struct dc_hw *hw, enum dc_hw_plane_id id,
 		      struct dc_hw_roi *roi);
 void dc_hw_update_colorkey(struct dc_hw *hw, enum dc_hw_plane_id id,
diff --git a/drivers/gpu/drm/eswin/es_drv.c b/drivers/gpu/drm/eswin/es_drv.c
index ac940763ef80..1ee7f00528c1 100644
--- a/drivers/gpu/drm/eswin/es_drv.c
+++ b/drivers/gpu/drm/eswin/es_drv.c
@@ -342,9 +342,7 @@ static int es_drm_bind(struct device *dev)
 	}
 	DRM_INFO("drm dev is on die%d\n", id);
 	priv->die_id = id;
-#ifdef CONFIG_ESWIN_MMU
 	priv->mmu_constructed = false;
-#endif
 
 	if (drm_dev->unique) {
 		sprintf(drm_dev->unique, "%d", id);
diff --git a/drivers/gpu/drm/eswin/es_drv.h b/drivers/gpu/drm/eswin/es_drv.h
index 856866f933ac..591405c8bc7e 100644
--- a/drivers/gpu/drm/eswin/es_drv.h
+++ b/drivers/gpu/drm/eswin/es_drv.h
@@ -29,9 +29,7 @@
 #include <drm/drm_device.h>
 
 #include "es_plane.h"
-#ifdef CONFIG_ESWIN_MMU
 #include "es_dc_mmu.h"
-#endif
 
 /*
  *
@@ -57,11 +55,8 @@ struct es_drm_private {
 	struct drm_property *async_commit_prop;
 	struct drm_property *share_id_prop;
 #endif
-
-#ifdef CONFIG_ESWIN_MMU
 	dc_mmu *mmu;
 	bool mmu_constructed;
-#endif
 	unsigned int die_id;
 
 	unsigned int pitch_alignment;
diff --git a/drivers/gpu/drm/eswin/es_gem.c b/drivers/gpu/drm/eswin/es_gem.c
index e44cfa325527..e10d2c784714 100644
--- a/drivers/gpu/drm/eswin/es_gem.c
+++ b/drivers/gpu/drm/eswin/es_gem.c
@@ -39,7 +39,6 @@ static void nonseq_free(struct page **pages, unsigned int nr_page)
 		__free_page(pages[i]);
 }
 
-#ifdef CONFIG_ESWIN_MMU
 static int get_pages(unsigned int nr_page, struct es_gem_object *es_obj)
 {
 	struct page *pages;
@@ -89,7 +88,6 @@ static int get_pages(unsigned int nr_page, struct es_gem_object *es_obj)
 
 	return 0;
 }
-#endif
 
 static void put_pages(unsigned int nr_page, struct es_gem_object *es_obj)
 {
@@ -109,9 +107,7 @@ static int es_gem_alloc_buf(struct es_gem_object *es_obj)
 	unsigned int nr_pages;
 	struct sg_table sgt;
 	int ret = -ENOMEM;
-#ifdef CONFIG_ESWIN_MMU
 	struct es_drm_private *priv = dev->dev_private;
-#endif
 
 	if (es_obj->dma_addr) {
 		DRM_DEV_DEBUG_KMS(dev->dev, "already allocated.\n");
@@ -136,19 +132,13 @@ static int es_gem_alloc_buf(struct es_gem_object *es_obj)
 					 &es_obj->dma_addr, GFP_KERNEL,
 					 es_obj->dma_attrs);
 	if (!es_obj->cookie) {
-#ifdef CONFIG_ESWIN_MMU
 		ret = get_pages(nr_pages, es_obj);
 		if (ret) {
 			DRM_DEV_ERROR(dev->dev, "fail to allocate buffer.\n");
 			goto err_free;
 		}
-#else
-		DRM_DEV_ERROR(dev->dev, "failed to allocate buffer.\n");
-		goto err_free;
-#endif
 	}
 
-#ifdef CONFIG_ESWIN_MMU
 	/* MMU map*/
 	if (!priv->mmu) {
 		DRM_DEV_ERROR(dev->dev, "invalid mmu.\n");
@@ -167,9 +157,6 @@ static int es_gem_alloc_buf(struct es_gem_object *es_obj)
 		DRM_DEV_ERROR(dev->dev, "failed to do mmu map.\n");
 		goto err_mem_free;
 	}
-#else
-	es_obj->iova = es_obj->dma_addr;
-#endif
 
 	if (!es_obj->get_pages) {
 		ret = dma_get_sgtable_attrs(to_dma_dev(dev), &sgt,
@@ -208,17 +195,14 @@ static int es_gem_alloc_buf(struct es_gem_object *es_obj)
 static void es_gem_free_buf(struct es_gem_object *es_obj)
 {
 	struct drm_device *dev = es_obj->base.dev;
-#ifdef CONFIG_ESWIN_MMU
 	struct es_drm_private *priv = dev->dev_private;
 	unsigned int nr_pages;
-#endif
 
 	if ((!es_obj->get_pages) && (!es_obj->dma_addr)) {
 		DRM_DEV_DEBUG_KMS(dev->dev, "dma_addr is invalid.\n");
 		return;
 	}
 
-#ifdef CONFIG_ESWIN_MMU
 	if (!priv->mmu) {
 		DRM_DEV_ERROR(dev->dev, "invalid mmu.\n");
 		return;
@@ -239,17 +223,18 @@ static void es_gem_free_buf(struct es_gem_object *es_obj)
 			}
 		}
 	}
-#endif
 
-	if (!es_obj->get_pages) {
-		dma_free_attrs(to_dma_dev(dev), es_obj->size, es_obj->cookie,
-			       (dma_addr_t)es_obj->dma_addr, es_obj->dma_attrs);
-	} else {
-		if (!es_obj->dma_addr) {
-			DRM_DEV_ERROR(dev->dev, "No dma addr allocated, no need to free\n");
-			return;
+	if(!es_obj->base.import_attach) {
+		if (!es_obj->get_pages) {
+			dma_free_attrs(to_dma_dev(dev), es_obj->size, es_obj->cookie,
+				       (dma_addr_t)es_obj->dma_addr, es_obj->dma_attrs);
+		} else {
+			if (!es_obj->dma_addr) {
+				DRM_DEV_ERROR(dev->dev, "No dma addr allocated, no need to free\n");
+				return;
+			}
+			put_pages(es_obj->size >> PAGE_SHIFT, es_obj);
 		}
-		put_pages(es_obj->size >> PAGE_SHIFT, es_obj);
 	}
 
 	kvfree(es_obj->pages);
@@ -259,13 +244,11 @@ static void es_gem_free_object(struct drm_gem_object *obj)
 {
 	struct es_gem_object *es_obj = to_es_gem_object(obj);
 
-#ifdef CONFIG_ESWIN_MMU
 	if (es_obj)
 		es_gem_free_buf(es_obj);
-#endif
-	if (obj->import_attach) {
+
+	if (obj->import_attach)
 		drm_prime_gem_destroy(obj, es_obj->sgt);
-	}
 
 	drm_gem_object_release(obj);
 
@@ -472,7 +455,6 @@ es_gem_prime_import_sg_table(struct drm_device *dev,
 	u32 i = 0;
 	dma_addr_t expected;
 	size_t size = attach->dmabuf->size;
-#ifdef CONFIG_ESWIN_MMU
 	u32 iova, j;
 	struct scatterlist **splist;
 	struct es_drm_private *priv = dev->dev_private;
@@ -482,7 +464,6 @@ es_gem_prime_import_sg_table(struct drm_device *dev,
 		ret = -EINVAL;
 		return ERR_PTR(ret);
 	}
-#endif
 
 	size = PAGE_ALIGN(size);
 
@@ -503,7 +484,6 @@ es_gem_prime_import_sg_table(struct drm_device *dev,
 		goto err_free_page;
 
 	expected = sg_dma_address(sgt->sgl);
-#ifdef CONFIG_ESWIN_MMU
 	splist = (struct scatterlist **)kzalloc(sizeof(s) * sgt->nents,
 						GFP_KERNEL);
 	if (!splist) {
@@ -528,23 +508,12 @@ es_gem_prime_import_sg_table(struct drm_device *dev,
 
 	for (j = sgt->nents; j > 0; j--) {
 		s = splist[j - 1];
-#else
-	for_each_sg (sgt->sgl, s, sgt->nents, i) {
-#endif
-		if (sg_dma_address(s) != expected) {
-#ifndef CONFIG_ESWIN_MMU
-			DRM_ERROR("sg_table is not contiguous");
-			ret = -EINVAL;
-			goto err;
-#endif
-		}
 
 		if (sg_dma_len(s) & (PAGE_SIZE - 1)) {
 			ret = -EINVAL;
 			goto err;
 		}
 
-#ifdef CONFIG_ESWIN_MMU
 		iova = 0;
 
 		if (j == 1) {
@@ -560,29 +529,20 @@ es_gem_prime_import_sg_table(struct drm_device *dev,
 
 		if (i == 0)
 			es_obj->iova = iova;
-#else
-		if (i == 0)
-			es_obj->iova = sg_dma_address(s);
-#endif
-
 		expected = sg_dma_address(s) + sg_dma_len(s);
 	}
 
 	es_obj->dma_addr = sg_dma_address(sgt->sgl);
 
 	es_obj->sgt = sgt;
-#ifdef CONFIG_ESWIN_MMU
 	kfree(splist);
-#endif
 
 	return &es_obj->base;
 
-#ifdef CONFIG_ESWIN_MMU
 err:
 	kfree(es_obj->iova_list);
 err_sp:
 	kfree(splist);
-#endif
 err_free_page:
 	kvfree(es_obj->pages);
 err_gemalloc:
diff --git a/drivers/gpu/drm/eswin/es_gem.h b/drivers/gpu/drm/eswin/es_gem.h
index 4dc902406052..4534d2a3f287 100644
--- a/drivers/gpu/drm/eswin/es_gem.h
+++ b/drivers/gpu/drm/eswin/es_gem.h
@@ -28,12 +28,10 @@
 
 #include "es_drv.h"
 
-#ifdef CONFIG_ESWIN_MMU
 typedef struct _iova_info {
 	u32 iova;
 	u32 nr_pages;
 } iova_info_t;
-#endif
 
 /*
  *
@@ -60,10 +58,8 @@ struct es_gem_object {
 	bool get_pages;
 	struct page **pages;
 	struct sg_table *sgt;
-#ifdef CONFIG_ESWIN_MMU
 	iova_info_t *iova_list;
 	u32 nr_iova;
-#endif
 };
 
 static inline struct es_gem_object *to_es_gem_object(struct drm_gem_object *obj)
-- 
2.54.0

